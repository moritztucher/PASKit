//
//  PASKitPurchases.swift
//  PASKitPurchases
//
//  Module scaffold. The RevenueCat dependency is wired in `Package.swift`;
//  the module API is intentionally not yet written.
//
//  Scope, design decisions, and the build plan live in `docs/PASKitPurchases.md`.
//  Per the build philosophy in `CLAUDE.md`, this module's API is designed
//  against a real consuming app — not in advance.
//

/// Namespace placeholder so the target compiles. Replace with the real surface
/// (RevenueCat facade, entitlement gating, hosted-paywall presentation) when the
/// first app integrates `PASKitPurchases`.
public enum PASKitPurchases {}
