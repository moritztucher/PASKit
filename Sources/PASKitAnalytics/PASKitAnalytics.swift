//
//  PASKitAnalytics.swift
//  PASKitAnalytics
//
//  Module scaffold. The PostHog dependency is wired in `Package.swift`;
//  the module API is intentionally not yet written.
//
//  Scope, design decisions, and the build plan live in `docs/PASKitAnalytics.md`.
//  Per the build philosophy in `CLAUDE.md`, this module's API is designed
//  against a real consuming app — not in advance.
//

/// Namespace placeholder so the target compiles. Replace with the real surface
/// (PostHog facade — configure, identify, register, reset, capture) when the
/// first app integrates `PASKitAnalytics`.
public enum PASKitAnalytics {}
